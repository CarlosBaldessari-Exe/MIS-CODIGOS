cohercion 
x:integer;
y; real;
begin 
x:=1;
y= x; Lo que va a guardar Y va a ser el valor de x(3.00)

x=y+1 esto no se puede hacer porque no se puede cambiar de real a entero 


Sobrecarga, coercion y precedencia de operadores
ejemplos
trunc(7.7)=7 
round (7.7)=8 
round (7.3)=7 
En si el trunc redondea al par mas cercano dependiendo el valor despues del (.)
round redondea al par mas cercano dependiendo el valor despues del (.)

==========================================
Tipos booleanos 
c,a,b:boolean;
begin
c:= a and b;
c:= a or b;
c:= not a; Si es true es false y not false es true

=========================================
Si quiero poner un valor por ej 3 tengo que ponerlo entre comillas '3'

ord ('1')
ord('0') 
La diferencia entre esos caracteres es 1, porque la tabla nos va a 

dar el valor de cada caracter y el caracter '1' es el primero y el caracter '0'
es el segundo por lo tanto la diferencia es 1.
==========================================

